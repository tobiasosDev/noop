<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (plain HTTP) is BLOCKED everywhere by default — every public endpoint (the cloud AI
  providers, anything else) must use HTTPS. The ONLY exception is loopback: the AI Coach's "Custom
  (OpenAI-compatible)" provider talks to a LOCAL LLM server (Ollama / LM Studio / llama.cpp) at
  http://localhost / 127.0.0.1, and the emulator reaches the dev host at 10.0.2.2. Those never leave
  the device/host, so cleartext to them is safe.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">::1</domain>
        <!-- Android emulator's alias for the host machine's loopback. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
