<?xml version="1.0" encoding="utf-8"?>
<!-- Home-screen widget metadata. updatePeriodMillis=0: the OS never polls us — the app pushes
     updates through WidgetSnapshotStore whenever live state or today's Rest/Charge/Effort changes (#516).
     minWidth bumped to ~2 cells so the three-score row has room at the smallest placement. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/glance_default_loading_layout" />
